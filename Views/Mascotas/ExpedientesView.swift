
import SwiftUI

struct ExpedientesView: View {
    var body: some View {
        Text("Expedientes")
            .font(.system(size: 22, weight: .bold, design: .rounded))
            .foregroundStyle(Color.brandPrimary)
    }
}
